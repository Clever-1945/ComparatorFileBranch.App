<UserControl 
    x:Class="ComparatorFileBranch.App.Controls.LeftToolsControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
    xmlns:local="clr-namespace:ComparatorFileBranch.App.Controls"
    mc:Ignorable="d" 
    d:DesignHeight="450" 
    d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="30"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="20"></RowDefinition>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="30"></ColumnDefinition>
            <ColumnDefinition Width="*"></ColumnDefinition>
        </Grid.ColumnDefinitions>

        <Button Grid.Row="0" Grid.Column="0" Padding="2" Margin="2" Click="OnSave">
            <Image Source="/Images/Save.png" />
        </Button>

        <TextBox
            Grid.Row="0"
            Grid.Column="1"
            Padding="2"
            Margin="2"
            Name="NameTextFilter"
            TextChanged="OnChangeFilter">
            <TextBox.Style>
                <Style TargetType="TextBox">
                    <Style.Triggers>
                        <Trigger Property="Text" Value="">
                            <Setter Property="Background">
                                <Setter.Value>
                                    <VisualBrush Stretch="None" AlignmentX="Left">
                                        <VisualBrush.Visual>
                                            <TextBlock Text="Фильтр" Foreground="Gray" FontStyle="Italic" Margin="5,0,0,0"/>
                                        </VisualBrush.Visual>
                                    </VisualBrush>
                                </Setter.Value>
                            </Setter>
                        </Trigger>
                        <Trigger Property="IsFocused" Value="True">
                            <Setter Property="Background" Value="White"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </TextBox.Style>
        </TextBox>
        

        <ListView 
            Grid.Row="1"
            Grid.Column="0" 
            Grid.ColumnSpan="2"
            ItemsSource="{Binding ListFilteredBranch}" 
            SelectedValue="{Binding CurrentBranch}"
            SelectionChanged="OnSelectedBranch"
            DisplayMemberPath="Name" />
        
        <TextBox
            Grid.Row="2" 
            Grid.Column="0" 
            Grid.ColumnSpan="2" 
            Text="{Binding TextInfo}"
            ToolTip="{Binding TextInfo}"
            IsReadOnly="True"
            />
    </Grid>
</UserControl>
